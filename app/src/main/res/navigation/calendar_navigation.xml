<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/calendar_navigation"
    app:startDestination="@id/calendarActivity">

    <activity
        android:id="@+id/calendarActivity"
        android:name="it.fnorg.bellapp.CalendarActivity"
        android:label="calendar_fragment_month_view"
        tools:layout="@layout/calendar_fragment_month_view" />
</navigation>