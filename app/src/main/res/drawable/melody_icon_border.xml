<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item>
        <shape>
            <solid android:color="@android:color/transparent" /> <!-- Trasparente all'interno -->
            <stroke
                android:width="2dp"
                android:color="@android:color/black" /> <!-- Bordi neri -->
        </shape>
    </item>

</layer-list>
